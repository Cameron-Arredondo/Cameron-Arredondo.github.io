source "https://rubygems.org"

# Use GitHub Pages environment — this automatically picks the right Jekyll and plugin versions
gem "github-pages", group: :jekyll_plugins

# Needed for serving locally (Ruby 3+ compatibility)
gem "webrick"
